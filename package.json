{
  "name": "puck-monorepo",
  "private": true,
  "scripts": {
    "dev:api": "bun --filter @puck/api dev",
    "dev:web": "bun --filter @puck/web dev",
    "dev": "bun --filter '*' dev",
    "test:api": "bun --filter @puck/api test",
    "lint": "prettier --check ./**/*.ts"
  },
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "3.8.0"
  }
}
