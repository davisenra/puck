{
  "name": "puck-monorepo",
  "scripts": {
    "dev:api": "bun --filter @puck/api dev",
    "dev:web": "bun --filter @puck/web dev",
    "dev": "bun --filter '*' dev",
    "test:api": "bun --filter @puck/api test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "3.8.0"
  }
}
