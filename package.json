{
  "name": "puck-monorepo",
  "private": true,
  "scripts": {
    "dev:api": "bun --filter @puck/api dev",
    "dev:web": "bun --filter @puck/web dev",
    "test:api": "bun --filter @puck/api test",
    "code-style": "prettier --check ./**/*.ts",
    "lint:web": "bun --filter @puck/web type-check"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "3.8.0"
  }
}
